{
  "name": "lws-vocab",
  "private": true,
  "scripts": {
    "build": "yml2vocab -v vocabulary -t template.html -c && mv vocabulary.html index.html"
  },
  "devDependencies": {
    "yml2vocab": "^1"
  }
}
